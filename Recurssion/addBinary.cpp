#include <bits/stdc++.h>
using namespace std;
void addbinary(string a, string b, int carry, string temp, int i, int j)
{
  int sum = 0;
  if (i >= 0)
  {
  }

  if (i < 0 && j < 0 && carry == 0)
  {
  }
}
